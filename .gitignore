# ===== 환경 변수 / 비밀 =====
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# ===== Python (backend, ai) =====
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# ===== Node / Frontend =====
node_modules/
.next/
out/
build/
dist/
.npm/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp.*

# ===== AI 모델 / 가중치 / 산출물 (용량 큼 — git 금지) =====
/models/
ai/**/models/
weights/
*.ckpt
*.safetensors
*.pth
*.onnx
*.gguf
ai/**/outputs/
ai/**/checkpoints/

# ===== 미디어 산출물 (영상/음성 — git 금지) =====
*.mp4
*.mov
*.wav
*.mp3
!frontend-rn/assets/audio/*.mp3
uploads/
generated/
ai/tts/_output/

# 개인 작업중 스크래치 메모 (devlog 정식 일지 아님)
docs/devlog/members/_*.md

# TTS 서비스 계정 키 (절대 커밋 금지 — 비밀)
ai/tts/*.json
gcp-tts-key.json

# ===== RAG 벡터 DB (로컬 영속 — git 금지) =====
# 벡터 DB 바이너리는 추적 안 함 → 각자 `cd ai && python -m rag.ingest`로 재생성.
ai/rag/_chroma/
ai/rag/eval/_chroma_eval/
# 코퍼스 텍스트 원본(corpus.json)은 팀 공유·diff·윤리경계 리뷰 위해 추적합니다.
# (민감 원문은 넣지 말 것 — 윤리 경계 안에서 공유 가능한 텍스트만)

# ===== Docker / DB =====
*.sqlite3
*.db
mongo-data/
data/db/

# ===== IDE / OS / 에이전트 =====
.vscode/
.idea/
.claude/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# ===== 예외: 문서용 샘플은 허용하려면 아래처럼 화이트리스트 =====
# !docs/assets/sample.mp4
nohup.out

# ===== AI 검증 스크립트 실행 로그 (validate_*.py 출력 덤프 · 테스트 아님 · 커밋 금지) =====
validate_*_result.txt
