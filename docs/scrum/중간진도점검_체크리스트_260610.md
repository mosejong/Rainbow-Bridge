# 2차 프로젝트 중간진도점검 체크리스트

**점검 일시:** 2026-06-10  **팀명:** 레인보우 브릿지 (팀 5)  **제출자:** 모세종 (PM)

**프로젝트명:** 레인보우 브릿지 — 반려동물 시한부 선고~이별~회복 AI 케어 서비스

---

## 1. 기획 및 페르소나

- **퍼르소나 한 줄 정의** — 완료
  - "시한부 선고를 받은 반려동물 보호자에게, 남은 시간을 함께 채우고 이별 후 일상을 되찾을 힘을 주는 AI 케어 서비스"

- **퍼르소나 카드 작성** — 완료
  - B2B2C 4자 역할(병원·장례·보호자·우리), 수익모델, 금기사항(1인칭 반려동물·부활 금지), 위기 시 1393 연결

- **타깃 사용자 및 핵심 사용 시나리오 3개** — 완료
  1. 시한부 진입 — 수의사 선고 직후 보호자가 버킷리스트·추억 기록 시작
  2. 이별 직후 — 임종 후 "공백 시간", 장례 안내 + 위로 메시지로 감정 지지
  3. 회복 단계 — 감정 체크인 3회 이상 + 회복 게이트 통과 → 일상 복귀 미션·추모 편지 해금

- **기존 서비스 대비 차별점** — 완료
  - 단독 앱이 아닌 B2B2C 연계형 (병원·장례업체가 채널, 보호자 무료)
  - 병원→장례 "공백" 시간을 직접 타깃으로 하는 서비스
  - 회복 게이트(`content_unlocked`) — 위기 상태 보호자에게 추모 콘텐츠 차단
  - 위기 감지 4단계(L0~L3) + 1393 자동 라우팅

- **결과물 형태 및 모달리티 선택 확정** — 완료
  - 텍스트: Gemini 2.5-flash 기반 추모 메시지
  - 음성: Qwen3 로컬 TTS (추모 톤 진중함 최우수, VoiceDesign instruct 튜닝 가능)
  - 영상: LivePortrait 사진→발화 영상 + FFmpeg faststart

---

## 2. 팀 이슈

- **역할 분담** — 완료
  - 모세종(PM·백엔드) / 김윤한(백엔드·인프라) / 반소람(AI LLM) / 정환주(AI TTS·GPU) / 민경이(프론트) / 장민수(멀티모달)

- **이슈·일정 관리 도구** — 완료
  - GitHub PR 리뷰·머지 기반 관리, 인당 1브랜치 전략, [docs/PROGRESS.md](../PROGRESS.md) 진행도 관리

- **1일 회고 및 역할 재조정** — 진행중
  - 개인 개발일지 일부 작성 중
  - 완료 계획: 발표 전까지 매일 개인 일지 작성 유지

---

## 3. AI 응답 엔진

- **응답 엔진 선정** — 완료
  - Gemini 2.5-flash (OpenAI 호환), 근거: 한국어 품질·무료 할당량

- **시스템 프롬프트 이상으로 페르소나 응답 동작 확인** — 완료
  - `ai/llm/prompts/` 분리 관리, 가드레일 테스트 통과, 위기 감지 골든셋 39건 미탐 0

- **엔진 환경변수 스위칭 구조** — 완료
  - `LLM_PROVIDER` / `LLM_BASE_URL` / `LLM_MODEL` 환경변수 분기
  - LLM 복원력 3건 대응 (강사님 6/9 지적): 429 키 순환 완료 / 모델 폴백(`LLM_FALLBACK_MODEL`, 전 키 소진 시 gemini-1.5-flash 재시도) + graceful 처리(LLMError → 안내문, 앱 500 미발생) — 완료(PR #150)

- **파이프라인** — 완료
  - memorial(추모 메시지) → RAG + Gemini + 가드레일
  - mission(미션 추천) → LLM + 규칙 폴백
  - safety(위기 감지) → 규칙(L0) + LLM JSON 분류(L1) → 4등급 판정
  - funeral / terminal_care (장례 안내·시한부 케어)

---

## 4. 백엔드 및 통합

- **API 서버 실제 응답 호출 전 과정 동작** — 완료
  - NCP 실서버 운영 중 (`https://rainbow-bridge.duckdns.org`), 헬스체크 확인

- **대화 저장소 구축 및 데이터 적재** — 완료
  - MongoDB(감정·메시지·타임라인·펫 프로필) + Redis(감정 체크인 7회 캐시)

- **비용·성능 절감 기법** — 완료
  - Redis 캐시(반복 DB 쿼리 절감), RAG(토큰 절약), Gemini 키 순환(무료 할당량 2배)

---

## 5. 클라이언트

- **UI-백엔드 연동** — 완료
  - React Native + Expo SDK 52, 실서버 URL 고정 (ngrok 하드코딩 제거 완료)

- **E2E 전 구간 연결** — 진행중
  - 로그인→프로필→감정 체크인→추모 메시지→TTS→영상 완주 확인
  - 버킷리스트·일기 화면 미착수
  - 완료 계획: 06-10 민경이 착수, 발표 전 E2E 재검증

---

## 6. 평가 설계

- **평가 지표 3~5개 및 가중치** — 완료

  | 지표 | 가중치 | 측정 방법 |
  |------|--------|-----------|
  | 위기 감지 미탐률 | 40% | 골든셋 39건 기준 L2+ 미탐 0 |
  | 메시지 가드레일 통과율 | 30% | 1인칭·부활 표현 미포함 비율 |
  | 회복 게이트 정확도 | 20% | 단위 테스트 8종 통과 |
  | TTS 톤 일치율 | 10% | warm·calm·soft 3톤 청취 검증 |

- **카테고리 균형 테스트셋** — 완료
  - corpus.json 75건 (memorial 45 / funeral 13 / mission 9 / comfort 8)
  - 위기 감지 골든셋 39건

---

## 7. 멀티모달

- **담당 모달리티 및 담당자** — 완료
  - 음성(TTS): 정환주 — Qwen3 로컬
  - 영상(LivePortrait): 장민수 — 사진→발화 영상, driving_multiplier 0.4

- **통합 일정** — 진행중
  - LivePortrait 파이프라인·FFmpeg faststart 완료
  - GPU remote 모드(정환주 터널 연결) 진행 중
  - 완료 계획: 최종 주차 이전 GPU 연결 완료, 지연 시 로컬 LivePortrait로 데모

---

## 8. 리스크 관리

- **블로커 식별 및 해결 담당** — 진행중

  | 블로커 | 담당 | 상태 |
  |--------|------|------|
  | Qwen3 TTS synthesize() 백엔드 연결 | 정환주 | 진행중 |
  | 버킷리스트·일기 화면 | 민경이 | 06-10 착수 |
  | GPU remote 연결 | 정환주 | 진행중 |

- **일정 지연 대응 계획** — 완료
  - TTS 백엔드 연결 지연 시: 기존 Google TTS API로 발표, Qwen3 후속 교체
  - GPU 연결 지연 시: 로컬 LivePortrait로 데모 (이미 동작 확인)
  - 버킷리스트·일기 지연 시: 핵심 8기능 E2E로 발표 커버
