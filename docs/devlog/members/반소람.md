# 개발일지 — 반소람 (AI 엔지니어)

> 최신 날짜를 **맨 위**에 추가하세요. 통합 일지: [../README.md](../README.md)
>
> <details><summary>📋 작성 템플릿 (복사해서 사용)</summary>
>
> ```markdown
> ## YYYY-MM-DD (Day N)
> ### 오늘의 목표
> -
> ### 한 일
> - [x]
> ### 막힌 것 / 고민
> -
> ### 내일 할 일
> - [ ]
> ```
> </details>

---

## 2026-06-08 (Day 6)

### 한 일
- [x] **L1/L2/L3 차등 응답 라우팅** — `safety.decide_action()`(CrisisAction 4종) 도입, 4개 생성 기능(③ 메시지·triage·funeral·anniversary)에 일관 적용
  - L0 평소 생성 / L1 생성+복지자원+공감우선 / L2 생성+1393 동봉(crisis_message)+공감우선 / L3 생성 전면중단·1393만(source=safety)
  - 🚨 L2(경고)에선 1인칭 편지 강제 차단(`first_person=False` 가드) — risk_level 0~1 조건 위반 방지
- [x] **복지자원(심리상담 9곳) 정리** — `WELFARE_RESOURCES`에 `scope`·`url`·`featured` 키 추가
  - `featured=True`(무료 공공+전국 2곳)는 상시 노출 / 나머지 7곳은 토글(프론트=민경이) / 유료는 변동성 고려 대략값
- [x] **검증** — `validate_crisis_routing.py` 신규(4기능×4등급), 전체 통과 / 테스트 254 passed

### 다음에 해야 할 일
- [ ] 응답 스키마 변경 공유 → 모세종·민경이 (L1: support_message+welfare_resources+risk_level / L2: crisis_message)
- [ ] `featured` 토글 기준 민경이에게 공유 (true=상시, false=토글)
- [ ] WELFARE_RESOURCES 비용·자격·URL 운영 전 팀 재확인
- [ ] (다음 작업) 질병 triage RAG → 미션 RAG 보강 → 유저 편지→AI 답장 순

---

## 2026-06-05 (Day 5)

### 한 일
- [x] **③ 위기 차단 문구 보강** — `_CRISIS_NOTICE` 앞에 "지금은 안전이 먼저입니다."
- [x] **1단계 증상 진료 안내(triage) 신규** — `prompts/triage.py`+`triage.py`, 심각도 4단계 자동 분류, Gemini 실호출·테스트·검증 완료
- [x] **2단계 장례 절차 상담(funeral) 신규** — `prompts/funeral.py`+`funeral.py`, 5단계 안내, note 있으면 Gemini 개인화·위기 시 1393 우선, 테스트·검증 완료
- [x] **RAG 연결(memorial)** — `retrieve()` few-shot 주입 + `generate_message()` 배선, corpus 56개 확장(ChromaDB), A/B 비교로 효과 확인
- [x] **③ 1인칭 편지 모드 smoke test** — 기본 6케이스 3인칭 가드레일 통과, 1인칭 3케이스 출력·위기차단 확인
- [x] **회복 추이(recovery_trend) 반영** — ⑤ 미션 난이도 추이 보정(`회복 중`↑/`주의 필요`↓), ③ 메시지 톤 캘리브레이션(위기 선체크 이후), `validate_recovery_trend.py` 5종 통과+LLM 톤 비교, 하위호환

### 막힌 것 / 고민
- Gemini 무료 티어 일일 한도(20회) — 키 교체로 해결
- 분당 5회 한도 → 딜레이 4초 부족, 15초로 조정
- eval이 운영 corpus.json을 아직 안 봄 → 정환주님 선행 필요 (corpus 작업 후 품질 확인 불가)

### 다음에 해야 할 일

**기능 (내 작업) — 우선순위 순**
- [x] **L1/L2/L3 차등 응답 라우팅 + 복지자원 안내** — 완료(→ Day 6)
- [ ] **질병 triage RAG 신규 구축** — ✅범위 확정("기본 처치+내원 유도", 상세 진단·처방 ❌) → 진행 가능. `vet_protocol` 코퍼스 + `triage.py` RAG 배선(현재 미사용) + 프롬프트 + 검증 스크립트 (大)
- [ ] **미션 RAG 코퍼스 보강** — `corpus.json` `mission` 5건·단일 토픽뿐 → 확장 + `validate_mission_rag.py` (中)
- [ ] **3단계 기념일 케어 알림** — 코드·테스트(`anniversary.py`)는 됐고 **백엔드 스케줄러 연동만 남음** (모세종)
- [ ] **유저 편지→AI 답장 (신규, 설계만 합의)** — 회복 절대점수 게이트(1인칭 AND 조건) + 편지 위기 선체크(L1 융합). 미정 4개 먼저 결정: THRESHOLD·stability 정의·데이터 최소기준·두 번째 축

**팀 협의 — 합의 완료 (06-08)**
- [x] recovery_trend 경로 통합 → **A안 확정.** 모세종이 backend `message.py`를 `ai/llm.generate_message()` 호출로 수정 (ai/llm 준비 끝 → 연동 후 동작 확인만)
- [x] 미션 recovery_trend 연결 → 김윤한이 backend `mission.py`에 trend 계산 추가
- [x] triage 범위 → CLAUDE.md 기준대로 확정("기본 처치+내원 유도"). ETHICS 문서 한 줄 추가는 PM

---

## 2026-06-04 (Day 4)

### 한 일
- [x] **⑤ 미션 추천** — 감정 점수 → 난이도 규칙 + LLM 개인화 + 규칙 폴백, 테스트 10종(115종 통과)
- [x] **⑦ LLM 분류 레이어(L1)** — `classify_with_llm()` + `assess_crisis()` 보수적 융합(`max(L0, L1)`), 미탐 0 보장, 테스트 10종(125종 통과)
- [x] `validate_crisis_llm.py` — 실제 Gemini 골든셋 수동 검증 도구

### 결정 사항
- L1 활성화는 팀 결정 — 백엔드가 `assess_crisis(note, generate=provider.generate)` 로 호출 시 켜짐

### 막힌 것 / 고민
- Gemini 무료 티어 레이트리밋 → 폴백으로 흡수(서비스 무영향), 시연 시 호출 텀 필요
- 미션 출력 스키마 → 모세종님이 `MissionItem` 추가로 해결(PR #54)

### 추가 개선 작업
- [x] **⑤ 미션 규칙 풀 확장** — 난이도별 5→10개(총 30개), 반복 추천 감소
- [x] **⑦ 위기 감지 골든셋 20→30개 확장** — 완곡 표현·함정 케이스 추가, 신호 사전 보강(깨지않았으면·끝났으면좋겠·방법도다정해 등), L0 미탐 0·과탐 0, 159종 통과
- [x] **③ 추모 메시지 품질 검증** — `validate_memorial.py` 신규. 실제 Gemini 6종(강아지·고양이·토끼·위기 차단) 전부 가드레일 통과 확인

---

## 2026-06-03 (Day 3)

### 한 일 (무엇이 바뀌었나)
- [x] **`provider.py` 신규** — LLM 호출 추상화. OpenAI 호환 방식으로 Gemini 연결
  (`generate(prompt, *, max_tokens, temperature, json_mode)`), 재시도·타임아웃·예외 graceful
- [x] **`config.py` 신규** — `.env` 기반 설정(provider/base_url/model/key/타임아웃/재시도/토큰/온도)
- [x] **thinking 제어 추가** — `gemini-2.5-flash`는 thinking 기본 ON이라 답변이 잘림 →
  `reasoning_effort="none"`(config `LLM_REASONING_EFFORT`)으로 꺼서 해결
- [x] **`tests/test_provider.py` 신규** — 응답·오버라이드·json_mode·thinking·재시도·키누락 10종
- [x] **`memorial`이 실제 동작 확인** — `generate=provider.generate` 주입 →
  실제 Gemini로 봄이·초코 위로 메시지 생성, 위기 입력은 1393 우선, 부활 출력 차단까지 라이브 검증
- [x] **백엔드 API 스키마 합의·반영** — 리뷰 피드백 반영해 입출력 키를 백엔드 DB 기준으로 정렬
  - `years`(숫자) → **`period`**(문자열), `score` → **`emotion_score`**, 출력 `hotline` → **`crisis_message`**
  - 출력이 백엔드 `MessageResponse`(content·tone·source·risk_level·crisis_message)와 일치 → 모세종님이 stub을 `generate_message` 호출로 교체만 하면 연동 완료
- [x] 전체 테스트 84종 통과 / ruff·black 통과

### 결정 사항
- 엔진: **Gemini API**(OpenAI 호환 엔드포인트, `gemini-2.5-flash`). 로컬 EXAONE은 폴백
- `.env`에 Gemini 설정 채움 — **키는 로컬만**(`.gitignore`로 커밋 제외)
- ③ 입출력 키 = 백엔드 DB 스키마 기준(`pet.period`·`emotion_score`·`crisis_message`) — 백엔드팀 합의

### 막힌 것 / 고민
- Gemini 정식 확정은 골든셋(1인칭 회피·위기 미탐 0) 통과 확인 후 — 1인칭은 3샘플 통과, ⑦ LLM 분류 레이어에서 정식 검증 예정
- 톤 키(warm·calm·hopeful)가 정환주님 TTS 톤과 동일 → ③↔④ 값 합의 필요

### 다음 할 일
- [ ] ⑤ 미션 추천(`mission.py`) 또는 백엔드 ③ API 연동 협의

---

## 2026-06-02 (Day 2)

**컨디션:** AI 파트 본격 착수 🟨

### 오늘의 목표
- 위기 감정 감지(⑦) 규칙 레이어 고도화 + 골든 테스트셋 구축
- 추모 메시지(③) 프롬프트 초안 작성 + 생성 로직 흐름 잡기

### 한 일
- [x] **⑦ 위기 감지 규칙 고도화** — `safety.py` 규칙 레이어(L0)
  - 표현 대상(subject: self/pet/other) 구분으로 오탐 방지 ("봄이가 죽었어요"=정상 L0)
  - 본인 욕구 어법("~고 싶다") 신호 사전 + 맥락 점수(시제·부정·강도) 보정
  - 골든 테스트셋으로 미탐 0 회귀 검증, `CRISIS_HOTLINE`(1393) 상수화
- [x] **③ 추모 메시지 프롬프트 초안** — `prompts/memorial.py`
  - 시스템 프롬프트에 윤리 경계(반려동물 1인칭·부활 금지) 명시
  - 톤 3종(warm·calm·hopeful, TTS 톤 매핑 대비), 추억 키워드 자동 삽입
  - `build_user_prompt` / `build_messages` (system+user)
- [x] **③ 추모 메시지 생성 로직** — `memorial.py` `generate_message()`
  - 흐름: 위기 선체크 → 프롬프트 조립 → LLM 호출(주입) → 후처리 가드
  - 위기 신호(L2↑) 시 메시지 대신 1393 안내 우선
  - 부활/1인칭 출력 차단·재생성(`GuardrailViolation`)
- [x] **테스트** — `tests/test_memorial.py` 10종(프롬프트 조립·흐름·안전·가드), 전체 74종 통과

### 결정 사항
- `provider.generate`를 **의존성 주입**으로 비워둠 → 엔진 확정 전 가짜 모델로 흐름·가드 검증, 확정 후 그대로 꽂기
- 프롬프트는 코드와 분리해 `prompts/`에서 버전 관리 (팀 규칙)

### 막힌 것 / 고민
- **provider.py 미구현** — 진짜 LLM 호출은 선행결정 A(엔진/모델)·B(통합 방식)가 정해져야 가능. **정환주님과 협의 + GPU 서버(RTX 5060) 셋업** 필요
- ③ 메시지 "품질"(실제 위로 톤·1인칭 안 나오는지)은 모델 연결 후에야 검증 가능

### 내일 할 일
- [ ] PERSO API 문서 확인 (0-6) — 입/출력 형식·인증·할당량 파악
- [ ] 정환주님과 로컬 LLM 엔진/모델(결정 A)·통합 방식(결정 B) 협의
- [ ] 위 작업 `bansoram` → `dev` PR
