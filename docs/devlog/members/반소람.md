# 개발일지 — 반소람 (AI 엔지니어)

> 최신 날짜를 **맨 위**에 추가하세요. 통합 일지: [../README.md](../README.md)
>
> <details><summary>📋 작성 템플릿 (복사해서 사용)</summary>
>
> ```markdown
> ## YYYY-MM-DD (Day N)
> ### 오늘의 목표
> -
> ### 한 일
> - [x]
> ### 막힌 것 / 고민
> -
> ### 내일 할 일
> - [ ]
> ```
> </details>

---

## 2026-06-12 (Day 10)

### 오늘의 목표
- 미션 문항 검수 가이드(06-11)에 정리해둔 문제들을 실제 규칙풀에 반영

### 한 일
- [x] **미션 규칙풀 정리 — 삭제·신규·재분류** (`mission.py`) — 라운드로빈은 개수 무관하게 동작. (현재 풀: gentle 20 / small 21 / active 18)
  - **삭제 4건**(중복·유사): `창밖 바라보기`(←`창가에 앉아 있기`와 동일)·`좋아하는 간식 차리기`(다른 간식·식사 미션과 겹침)·`친구와 짧은 통화`(active, 다른 통화/만남 미션과 겹침)·`좋아하는 공간 가보기`(active, `가까운 곳 다녀오기`와 겹침). gentle `오늘 먹은 것 적기`는 `오늘 마음 한 줄`로 교체.
  - **신규 3건**: `대화방 한 번 열어보기`(gentle/connection — 연락 부담 없이 "혼자가 아니다" 느끼기)·`따뜻한 물로 샤워하기`(small/rest — 감각 이완)·`펫로스 위로 글 읽기`(small/connection — 애도 정상화).
  - **카테고리 재분류**: `좋아하는 사진 저장`·`추억 사진 정리`·`추억 영상 만들기` record→remembrance(지속적 유대), `오늘 잘한 일`→`오늘 한 일` record→rest(자기돌봄).
  - **난이도 조정**: active `가벼운 운동 30분`→`15분`(허들 낮춤).
- [x] **전제/조건 미션 graceful화** — 자원·환경이 없으면 미션을 아예 못 하던 문제 해소. 미션 UI가 「완료했어요」 체크만 있고 건너뛰기가 없어, 못 하는 미션이 뜨면 그대로 박혀 완료율을 깎는 구조라 문구로 보완:
  - 식물: gentle `화분에 물 주기`는 "없다면 가까운 초록 식물을 바라봐도" 대안 추가 / active `작은 화분 돌보기`→`작은 화분 하나 들이기`(이미 키우는 전제 제거 + 중복 해소)
  - 연락(connection): "연락하고 싶은 사람이 있다면 / 편한 사람이 떠오르면" 권유형으로 — 관계 부재 보호자 배려
  - 날씨: `집 앞 5분 산책` "괜찮으면 집 근처를, 아니면 집 안을"로 실내 대안
  - 낮잠: "잠이 안 오면 눈만 감고 쉬어도 좋아요"(불면 배려) / 편지 읽기: "써둔 편지가 있다면"
- [x] **자책 역효과 완화** — `오늘 잘한 일 하나`→`오늘 한 일 하나`. "잘한 일"이라는 성취 기준은 안 떠오를 때 자존감을 깎을 수 있어, "'그래도 이건 했네' 싶은 것 떠올리기"로 바를 낮춤. (가장 취약한 gentle엔 원래 없던 점도 확인)
- [x] **글쓰기 미션 11건 → 메모장 작성으로 명확화** — 우리 앱엔 사용자가 글을 입력하는 기능이 없어서, "적어보세요"가 앱 입력으로 오인됨. "**휴대폰 메모장 등 본인이 다시 볼 수 있는 곳에 적기**"로 통일(산책·정리처럼 오프라인+체크 구조와 동일). `prompts/mission.py` 시스템 프롬프트에도 같은 규칙을 넣어 LLM 생성 미션도 일관되게. → `record` 카테고리의 **표현적 글쓰기 발표 근거는 그대로 보존**.
- [x] **미션 테스트 22건 통과** — 풀 개수·문구 변경 후 회귀 확인.
- [x] **이름 조사(받침) 자동 교정 — 추모·장례 프롬프트** — 이름 뒤 조사가 받침과 무관하게 하드코딩돼 있어 "구름는/구름가", "콩와의/콩를"처럼 어색하게 나오던 문제 수정.
  - `prompts/memorial.py`: `_has_batchim`(받침 판단 `(ord(글자)-0xAC00) % 28 != 0`)·`_josa(word, "은는")` 유틸 추가. 템플릿의 `{name}는`·`{name}가`를 받침 맞춰 채우도록 플레이스홀더화. 보호자 별명 호명도 `이(가)` 병기 대신 정확한 조사로(`하늘님이`).
  - `prompts/funeral.py` + `funeral.py`: **여기가 더 심각** — 장례 단계 고정 안내(`STEP_TEMPLATES`)는 LLM을 안 거치고 그대로 사용자에게 노출되는데 `와`·`를`·`가`가 하드코딩이라 받침 이름이 깨졌음(기존 테스트는 "봄이"=받침 없음이라 우연히 통과). 템플릿을 `과/와`·`을/를`·`이/가` 양형으로 바꾸고, format 직후 anniversary의 검증된 `apply_josa`를 재사용해 교정.
  - 회귀 테스트 추가(`test_funeral.py`: 콩→콩과의/콩을, 코코→코코와의/코코가). 추모·기념일·장례 테스트 86건 통과.

### 막힌 것 / 고민
- **미션 건너뛰기 / 다른 미션 보기 버튼**(프론트) — 조건이 안 맞는 미션이 박히는 문제의 근본 해결책. 문구 완화는 대증요법일 뿐이라 결국 버튼이 필요. **민경이님(프론트) 결정 대기 → 논의중**, 정해지면 공유받기로 함.
- 글쓰기 미션을 "비-글쓰기로 전환 vs 삭제 vs 오프라인(메모장)" 중 무엇으로 할지 고민했는데, `record`=표현적 글쓰기가 발표 근거 기둥이라 **메모장 작성**으로 결정(근거 보존 + 입력 기능 불필요).

### 다음 할 일
- [ ] 미션 건너뛰기 버튼 프론트 결정 받기(민경이님)
- [ ] 논문 더 찾기(행동활성화·자기자비 우선) + 펫로스 직접 연구 탐색

---

## 2026-06-11 (Day 9)

### 한 일
- [x] **자책감 감지 → 위기 조합 상향** (`safety.py`·`memorial.py`) — 자책 마커 룰 추가: 융합 결과가 `risk_level >= L1` 이고 `subject == self` 이며 자책 표현이 있으면 한 단계 상향(min L2). **자책 단독은 무동작**(조합 가드로 오탐 봉쇄 — "미안해" 같은 약한 표현은 제외). memorial 추모 공감 톤도 반영. 골든셋 테스트 추가. (커밋 `3f00be4`)
- [x] **미션 수면 질 modifier** (`mission.py`) — 수면 5단계 입력 → 미션 난이도 3축약 보정: **1·2 → −1티어 / 3 → 유지 / 4·5 → +1티어**. 감정점수 기반 난이도 위에 얹는 보정(추이 modifier 옆). **회복 점수엔 미반영**(미션 강도 보조 전용). 테스트 추가. (커밋 `4889228`)
- [x] **회복점수·미션·수면·자책 결정 정리 문서** — `docs/scrum/회복점수_미션_결정_260611.md`. 회복 산식(누적 모델: 감정40+꾸준함25+미션누적35, 80 해금)·수면 신호 용도(점수 제외·미션 강도 보조)·자책 risk 룰 확정 기록. (커밋 `2ca0a2c`)
- [x] **미션 규칙풀 정리 (9건 교체)** — 물/차 중복·애매하거나 모호한 미션을 다른 행동으로 교체. gentle 햇빛 1분→창가 볕, small 차 한 잔→집안일/낮잠, active 추억앨범 중복·"가볍게 몸 움직이기"(모호) 등. 난이도별 20개·중복 제목 0 유지, 미션 테스트 통과. corpus 15건과 겹치던 미션 위주로 교체해 추천 다양성 확보 (커밋 `134c484`)
- [x] **미션 문항 검수 가이드 작성 → 팀원 인계** — `docs/scrum/미션문항_검수가이드_260611.md`. 규칙풀 60문항을 전수 점검해 문제·수정안·추가안을 정리해 인계(실제 문항 수정·자기자비 보강은 **팀원이 진행 예정, 아직 미구현**). 가이드 내용:
  - ①연락(connection) 미션 재검토 — "가족/친구" 단정→"편한 사람"으로 일반화(혼자 살거나 관계 부재인 보호자 배려), "동물 나눔 알아보기" 펫로스 직후 민감성 재검토
  - ②날씨 의존 미션 조정 — 실외 산책·볕 등 문구 완화(indoor/outdoor 태그는 백로그)
  - ③중복·카테고리 애매·정서 강도 문제 정리
  - ④자기자비(rest) 미션 추가 제안 — rest가 감각·신체(물·볕·향)에만 쏠려 "나를 향한 따뜻한 말"이 비어 있음. ⚠️ **오해 소지 경고 포함**: "괜찮아 그럴 수 있어"(감정 무시)·"내 탓이 아니야"(사실 단정→자책 자극)는 금지, **애쓴 것·사랑한 것 인정** 방향으로("오늘도 애썼어"·"해준 것 떠올리기")
  - + 테스트 참조 제목(쌍 OR 조건) 주의사항·작업 체크리스트
- [x] **신호→점수 치환 설계 갭 식별** — Expo 기기 신호(수면·활동량 등)를 피처로 만들어 점수로 치환하는 규칙이 통째로 없음을 확인. 절대 임계값 대신 **개인 14일 베이스라인 대비 변화율**로 정규화하는 방향 제시. Expo Go 실측 가능 범위를 웹검색으로 검증: 걸음수(Pedometer)는 되지만 **과거 7일치만 보관**(14일 베이스라인은 앱이 직접 누적해야 함), 수면·심박은 HealthKit/Health Connect라 **Expo Go 불가(dev build 필요)**. → 자동실측(걸음·움직임·재생률) + 자가응답(수면·사회생활·자책) **하이브리드**로 정리. 모세종님(회복점수 산식)·민경이님(데이터 수집) 공동 논의 필요
- [x] **활동 신호 ↔ 활동 미션 양방향 연관 설계** — 신호→미션(난이도 조정) + 미션→신호(수행 검증)의 양방향. 단 펫로스 맥락이라 검증은 "했으면 긍정 인정, 안 해도 무탈"(독촉·불이익 금지) 원칙으로 한정
- [x] **LivePortrait GIF 생성 404 장애 진단** (장민수님 영역 지원) — NCP에서 GIF 13건 전부 실패 신고. ngrok URL·로컬 서버·GPU 전부 정상인데도 실패. 원인 추적: 돌고 있던 LP 서버가 어제 띄운 구버전이라, 오늘 오전 추가된 `POST /generate/gif` route가 없어 404. `openapi.json`(서버 실제 route=`/generate`·`/health`만)·프로세스 시작시각·커밋 `d9487f6`로 확정. **서버 재시작**으로 해결(절차를 호스트(반소람 PC)에 전달). 근본 원인=코드 갱신 후 서버 재시작 누락 → 체크리스트화 권고

### 막힌 것 / 고민
- 신호→점수 치환식은 모세종님 회복점수 산식·민경이님 Expo 데이터 수집 가능 여부와 엮여 단독 결정 불가 → 다음에 셋이 합의 필요.

### 다음 할 일
- [ ] 논문 더 찾기(행동활성화·자기자비 우선) + 펫로스 직접 연구 탐색
- [ ] 미션 프롬프트(`prompts/mission.py`) 재검토 (※미션 문항 수정·자기자비 보강은 검수 가이드로 팀원에게 인계 → 팀원 진행)
- [ ] (구현 대기) 수면 modifier·자책 키워드→risk·미션 누적 산식

---

## 2026-06-10 (Day 8)

### 한 일
- [x] **미션 5분류 논문 근거 전부 보강** — 강사 "논문이 기능에 안 녹았다" 후속. 기존엔 5개 분류 중 2개만 출처가 있었는데, 5개 모두 구체적 논문(PubMed/PMC ID)·핵심 수치까지 채움. `docs/scrum/발표준비_논문근거.md` "카테고리별 논문 출처" 표 신설(카테고리|이론|논문|논문에서 어떻게 나타나는가 4열) + 논문/ID에 클릭 가능한 링크 부착
  - 기록/정리 ↔ 표현적 글쓰기(RCT 13건 메타분석, 비탄 Hedges' g=0.388)
  - 추모/기억 ↔ 지속적 유대(Field et al., 내면화=긍정 적응 / 외부화=고통 연관)
  - 가벼운 활동 ↔ 행동활성화(Papa 2013·Eisma 2015, d=0.8~0.9)
  - 사람과 연결 ↔ 사회적 지지(보호 요인)
  - 휴식/자기돌봄 ↔ 자기자비(COMPACT 타당성 시험)
  - ⚠️ **정직 표기 유지**: 5건 모두 인간 사별 연구(펫로스 직접 연구 아님) → "방법론적으로 검증된 개입을 적용"으로 표현. 행동활성화는 병리적/복합 비애 대상이었음도 표 안에 명시(과대주장 방지)
- [x] **추모 편지 마지막 줄 보호자 이름 버그 수정** — `prompts/memorial.py` 마무리 두 줄 지시에 "○○는 반드시 반려동물 이름, 보호자 이름·별명은 절대 금지" 명시(1·3인칭 둘 다). 별명 호명 적용 후 마지막 줄에 보호자 이름이 새던 케이스 차단 (커밋 `505a5ad`)
- [x] **정환주 ⑧ 리포트 집계 반영 확인** — 일상복귀 신호(`recovery_signal`) 통합 + `access_logs` 일별 버킷팅으로 접속빈도 근거 추가됨
- [x] **②번 정량 분석(일상복귀 신호) 마무리** — 반소람 몫(recovery_signal.py)·정환주 몫(report.py 통합) 모두 완료. 김윤한께 재생 이벤트 로그(개별 타임스탬프) 추가 가능 여부 문의 전달
- [x] **AI 위로 편지(유저 입력 없이 먼저 써줌) 완료** — 엔진(`generate_message`)·회복 게이트(`get_recovery`)·프론트 진입점(`message.jsx`)은 기존 구현 확인, 백엔드 연결(스키마 `request_first_person`/`content_unlocked`/`allow_first_person` + `create_message()` 게이트 호출)은 정환주가 구현
- [x] **미션 RAG corpus 난이도별 보강 (5→15건)** — 갭분석 후속(미션 corpus 데이터 부족 지적). 5개 카테고리(rest·remembrance·connection·activity·record) × 3개 난이도(gentle·small·active) = 15건으로 확장. 각 미션에 `difficulty`·`mission_category` 메타데이터 부착(`category`는 RAG 필터 키라 "mission" 유지) (커밋 `e17d082`)
- [x] **미션 RAG 검색에 난이도 필터 추가** — 전엔 `category=mission`만으로 검색해 무기력한 보호자에게도 active 미션이 섞여 나옴. 감정점수→난이도 매핑 후 `{"$and":[{"category":"mission"},{"difficulty":X}]}`로 검색(키 2개라 `$and` 필요). 실패 시 graceful fallback 유지. 테스트 2종 추가(난이도 필터·점수별 난이도 변화), 미션+RAG 16건 통과 (커밋 `e17d082`)
- [x] **RAG 임베딩 키 파싱 버그 수정** — `ingest`가 `400 Invalid Auth key`로 실패하던 원인 추적: `LLM_API_KEY`는 429 로테이션용으로 키 5개가 쉼표로 묶여 있는데, RAG config만 이를 안 쪼개고 통째로 임베딩 키로 넘기고 있었음(LLM config는 `split(",")`로 처리). 키가 무효가 아니라 5개를 한 덩어리로 보낸 게 무효였던 것. `_first_api_key()` 추가해 첫 유효 키만 사용 → 재적재 성공(73건), 난이도별 검색 검증 완료 (커밋 `1b90d4e`)

### 막힌 것 / 고민
- (해결됨) `ingest` `400 Invalid Auth key` — 키 무효가 아니라 쉼표 묶음 키 전체를 한 키로 넘긴 게 원인이었음. RAG config 키 파싱을 LLM쪽과 통일해 해결.

### 다음 할 일
- (없음)

---

## 2026-06-09 (Day 7)

### 한 일 (06-08 후반 작업 마무리)
- [x] **triage(LLM 일반 증상 Q&A) 제거** — 시나리오 변경(시한부 선고 이후 케어로 집중) 반영. `triage.py`·`prompts/triage.py`·`tests`·`validate_triage*` 삭제, 위기 라우팅 점검 3기능으로 (커밋 `fda274e`)
- [x] **시한부 케어 고정 안내 신규** — `terminal_care.py` + `data/terminal_care.json` + 테스트 6. 초기~중기 / 말기~임종 직전 증상 + 보호자 케어. **LLM 미사용**(고정 텍스트), disclaimer(AI 진단·예측 아님) + 1393은 `CRISIS_HOTLINE` 상수 주입 (커밋 `5afb9f7`)
- [x] **추모 프롬프트 1·3인칭 확정·동결** 🔒 — 버킷리스트·일기 자연 인용 / "말할 순 없었지만 기억으로 남겼습니다" / 보호자 걱정 마무리. 3인칭 기본 + 1인칭(동의+경고+risk 0~1) (커밋 `f9c4021`)
- [x] **버킷리스트·호칭(caller_name) 주입** — `generate_message`가 `pet`에서 꺼내 프롬프트 + RAG 쿼리로. 테스트 24, 전체 276 통과
- [x] **comfort RAG 단계별 보강** — 새 category `comfort` + `stage`(terminal/imminent) 분리(memorial 오염 방지), 12건 추가(63→75), 재적재·검색 검증. ⚠️ 코퍼스·벡터DB는 gitignore(로컬만)
- [x] **validate 실행로그(txt) 정리** — 10개 삭제 + `.gitignore` 등록(`validate_*_result.txt`) (커밋 `4b009ec`)
- [x] **corpus.json git 추적 전환 + 코퍼스 75건 팀 공유** — `.gitignore`에서 `corpus.json` 제거(텍스트 원본 추적 → diff·윤리경계 리뷰 가능). 벡터DB(`_chroma`)는 ignore 유지, 각자 `cd ai && python -m rag.ingest`로 재생성. 75건(memorial 45/funeral 13/mission 9/comfort 8) 공유 (커밋 `0b026c1`)
- [x] **`KIND_TRIAGE` → `KIND_VET_PROTOCOL` 개명** (`ai/evaluation/logs.py`) — triage 생성기능 드랍 후 미사용 상수를 수의사 처치안내 RAG 로깅용으로 정리(루트 RAG category와 통일). 잔존 참조 0, import 검증 통과 (커밋 `4bfba8a`)
- [x] **1인칭 화법 허용 기준 강화 문서화** — 회복 게이트 결정(최근 체크인 창 전체 `risk=0`) 반영해 `CLAUDE.md`·`ETHICS_추모표현_가이드.md`의 1인칭 기준을 `risk_level 0~1` → `0`으로 통일. 일반 추모 컨텐츠(`content_unlocked`)는 `0~1` 유지 (커밋 `38ba40f`)
- [x] **LLM 모델 폴백** — 강사님 6/9 피드백 #2. 전 키 429 소진 시 `LLM_FALLBACK_MODEL`(gemini-3-flash)로 전체 키 재시도(할당량은 모델별 별도). `config`+`provider` 모델 루프, `.env.example` 추가, 테스트 2종 (커밋 `322b16b`)
- [x] **LLM 실패 graceful 처리** — 강사님 피드백 "상위 graceful". `generate()` 최종 실패(LLMError) 시 앱 500 대신 `source:"unavailable"` 안내문 반환. memorial·funeral·anniversary 적용(가짜 추모글 금지). `LLM_UNAVAILABLE_NOTICE` 상수, 테스트 +1, 전체 280 통과 (커밋 `d9c8b6e`)

#### (오후·추가) 강사 6/9 피드백 후속 — 미션 근거화 + 주제 집중
- [x] **comfort·생전 미션 제거** — 오전에 넣은 `comfort`(8건)·생전 버킷리스트 미션(`stage:terminal` 4건) 삭제. **"미션=반려동물이 떠난 뒤 일상복귀"** 로 범위 확정(생전 위로는 미션 아님). corpus 75→63(memorial 45/funeral 13/mission 5), 벡터DB도 ID 삭제(임베딩 키 불필요)
- [x] **미션 분류별 회복 근거(rationale) 부착** — 강사 "논문이 기능에 안 녹았다" 대응. 5개 분류 ↔ 5개 회복 이론(행동활성화·지속적유대·표현적글쓰기·사회적지지·자기돌봄). `prompts/mission.py` `CATEGORY_RATIONALE` 에서 **모든 미션(규칙풀+LLM 생성)에 자동 부착**(LLM 환각 아닌 큐레이션 근거) + 프롬프트에도 노출. 테스트 +2, 11 통과
- [x] **미션 규칙 풀 30→60개 확장** — 난이도별 10→20개, 빈약 분류 보강(연결 3→9·추모 5→10). 중복 제목·금지어 0
- [x] **RAG 문서화 2종** — 개발용 [`ai/rag/RAG.md`](../../../ai/rag/RAG.md)(파이프라인·corpus 추가법·설정), 발표용 [`docs/RAG_사용처.md`](../../RAG_사용처.md)(RAG **3곳**(추모·미션·장례) 명확화 + `comfort`=코드상 `memorial`·`vet_protocol`=RAG 아님 정정)
- [x] **②번 정량 분석 틀** — "일상복귀 신호"(회복중/유지/주의) 순수함수 `ai/evaluation/recovery_signal.py`(감정추이+미션완료율, 접속/재생 빈도는 graceful) + 발표용 시뮬 데모 `demo_recovery_signal.py`("감정↑·접속↓=회복" 곡선). report.py 안 건드리고 새 파일 분리(정환주 협업), 테스트 8 통과. 피드백 "정량 입증" 대응
- [x] **추모(3인칭) 별명 본문 전체 일관 호명** — 글 첫머리에서만 별명("○○님")을 부르고 본문 묘사에서는 보호자 호칭(엄마·아빠)으로 돌아가던 문제 수정. 본문 묘사 줄에 `guardian_ref`(별명 있으면 "○○님", 없으면 `caller_name`) 주입 + 시스템 프롬프트·greeting_block에 "중간·끝에서도 같은 별명으로만" 지시 강화. 1인칭 모드는 기존대로 caller_name 사용(별명 무시). 본문까지 별명 통일 검증 테스트 추가, ai/llm 283 통과

### 막힌 것 / 고민
- ~~코퍼스(`corpus.json`)·벡터DB가 gitignore → RAG 추가분이 팀에 공유 안 됨. 팀 공유 방식 결정 필요(정환주+PM)~~ → **해결**: corpus.json은 추적, 벡터DB는 ingest 재생성 방식으로 확정
- Gemini 무료 한도(분당 5·일일 20) — 예시 점검 시 호출 간 딜레이 필요

### 다음 할 일
- [ ] 미션 코퍼스 회복 단계별(초·중·후기) 확장 + recovery_trend 난이도 연계
- [ ] **AI 위로 편지 (유저 입력 없이 AI가 먼저 써줌)** — 기존 `generate_message`(1·3인칭) 활용. 유저 편지 위기 선체크는 불필요(드랍). 1인칭 회복 게이트(consent+risk≤1+readiness) 적용 여부만 확인
- [ ] 시한부 케어 백엔드(`get_terminal_care_info()` 서빙)·프론트 연동 + 수의사 감수
- [ ] ②번 정량 분석 — report.py 통합(정환주): `access_logs` 버킷팅 + `compute_recovery_signal` 호출 (틀·시뮬 데모는 완료)

---

## 2026-06-08 (Day 6)

### 한 일
- [x] **L1/L2/L3 차등 응답 라우팅** — `safety.decide_action()`(CrisisAction 4종) 도입, 4개 생성 기능(③ 메시지·triage·funeral·anniversary)에 일관 적용
  - L0 평소 생성 / L1 생성+복지자원+공감우선 / L2 생성+1393 동봉(crisis_message)+공감우선 / L3 생성 전면중단·1393만(source=safety)
  - 🚨 L2(경고)에선 1인칭 편지 강제 차단(`first_person=False` 가드) — risk_level 0~1 조건 위반 방지
- [x] **복지자원(심리상담 9곳) 정리** — `WELFARE_RESOURCES`에 `scope`·`url`·`featured` 키 추가
  - `featured=True`(무료 공공+전국 2곳)는 상시 노출 / 나머지 7곳은 토글(프론트=민경이) / 유료는 변동성 고려 대략값
- [x] **검증** — `validate_crisis_routing.py` 신규(4기능×4등급), 전체 통과 / 테스트 254 passed

#### (오후) 위기감지 미탐·오탐 정리 + 가드 버그 수정
- [x] **위기감지 완곡표현 미탐 보강** — `safety._SIGNAL_TABLE`에 완곡 사망/소진/무가치 패턴 다수 추가. 원칙: **애매한 표현(피로·이벤트 회피·애도와 겹침)은 L1(공감), 명확한 죽음 완곡만 L2.**
  - 오탐 방지: 패턴 좁힘(`더이상/더는 버틸 힘이 없`) + 등급 하향(`그만 쉬고 싶`·`내일이 안 왔으면`·`이 세상에 없었으면` → L1). "회사 그만두고/주말에 쉬고/이런 슬픔이 세상에 없었으면" 등 일상·애도 표현이 1393(L2)로 가지 않게 확인
- [x] **memorial 1인칭 가드 부분일치 오탐 수정** — `"빛나는"`의 `나는`, `"문제가"`의 `제가` 같은 substring 오탐 → **단어경계 정규식**(`_FIRST_PERSON_RE`)으로 교체. 운영 가드(`memorial.py`) + 점검 스크립트(`validate_memorial.py`) 둘 다
- [x] **위기 골든셋 +10 케이스** 회귀 고정 — 규칙이 새로 잡는 케이스는 'L0 미탐 허용' 꼬리표 제거해 정식 회귀 대상으로 승격. **테스트 254 → 289 passed**
- [x] **funeral.py 데모 점검** — 오프라인(템플릿·위기차단 L3·RAG funeral 13개) + 실 Gemini 5단계 품질 통과. "병원→장례 공백" 진입 시 **장례 코어로 검증 완료**
- [x] **미션·보상 설계 노트** 작성(`ai/llm/미션_보상_설계.md`) — 정본 `docs/RECOVERY_GATE.md` 보완(점진 해금·미션 A/B·미션=재료). **미구현·왁구 확정 후 보류**

### 막힌 것 / 고민
- Gemini 무료 일일 한도(20/일) 소진 → triage·RAG 등 **LLM 예시 답안 점검 일부 미완**(429). 한도 리셋 후 재확인 필요
- 규칙(L0)은 완곡 표현을 다 못 잡음(설계상 LLM L1이 보완) — but LLM이 레이트리밋으로 자주 실패 → **규칙 안전망을 두껍게** 한 이유

### 다음에 해야 할 일

**이번 PR 후속 (팀 공유)**
- [x] **응답 스키마 공유 → 김윤한 (완료)** — 스크럼 2026-06-08 안건 #1·#4 공유 + 백엔드 반영 확인(`message.py`가 `support_message`·`welfare_resources` passthrough, `schemas/message.py` 필드 정의)
- [ ] **welfare_resources 프론트 토글 연결 (민경이)** — `featured`(true=상시/false=토글) 기준 공유. 현재 프론트는 mock→백엔드 API 연결 대기
- [ ] **L2 동작 결정 반영** — 스크럼 안건 #1: L2가 "생성+1393 동봉"(문서)이냐 "차단"(현 코드 `HOTLINE_REQUIRED_FROM=L2`)이냐 → 스키마에 직결
- [ ] **② 감정 체크인 경로 점검** — `emotion.py`는 `crisis_message`만 자체 생성, L1 `support_message`/`welfare_resources` 미passthrough → 의도 여부 확인
- [ ] WELFARE_RESOURCES 비용·자격·URL 운영 전 팀 재확인

**기능 백로그 (내 작업) — 우선순위 순** *(Day 5에서 이어짐)*
- [ ] **질병 triage RAG 신규 구축** — 범위 확정됨("기본 처치+내원 유도", 상세 진단·처방 ❌). `vet_protocol` 코퍼스 + `triage.py` RAG 배선(현재 미사용) + 프롬프트 + 검증 스크립트 (大)
- [ ] **미션 RAG 코퍼스 보강** — `corpus.json` `mission` 5건·단일 토픽뿐 → 확장 + `validate_mission_rag.py` (中)
- [ ] **3단계 기념일 케어 알림** — 코드·테스트(`anniversary.py`)는 완료, **백엔드 스케줄러 연동만 남음** (김윤한)
- [ ] **유저 편지→AI 답장 (신규, 설계만 합의)** — 회복 절대점수 게이트(1인칭 AND 조건) + 편지 위기 선체크(L1 융합). 미정 4개 먼저 결정: THRESHOLD·stability 정의·데이터 최소기준·두 번째 축
- [ ] **memorial 1인칭 가드 — `나는` 동사형 오탐 보강 (비차단 후속, 리뷰 코멘트)** — `"봄이는 하늘을 나는 것 같았어요"`·`"훨훨 나는 모습"`처럼 동사 '날다'의 `나는`을 1인칭으로 오탐(추모 핵심 이미지라 실제로 터질 수 있음). 정규식만으론 대명사 `나는`과 완벽 구분 불가(형태소/LLM 영역) → `을/를/훨훨/높이 + 나는` 부정형으로 흔한 케이스 줄이고, golden case 추가(`"봄이는 하늘을 나는 것 같았어요"` = 비차단)로 회귀 고정

---

## 2026-06-05 (Day 5)

### 한 일
- [x] **③ 위기 차단 문구 보강** — `_CRISIS_NOTICE` 앞에 "지금은 안전이 먼저입니다."
- [x] **1단계 증상 진료 안내(triage) 신규** — `prompts/triage.py`+`triage.py`, 심각도 4단계 자동 분류, Gemini 실호출·테스트·검증 완료
- [x] **2단계 장례 절차 상담(funeral) 신규** — `prompts/funeral.py`+`funeral.py`, 5단계 안내, note 있으면 Gemini 개인화·위기 시 1393 우선, 테스트·검증 완료
- [x] **RAG 연결(memorial)** — `retrieve()` few-shot 주입 + `generate_message()` 배선, corpus 56개 확장(ChromaDB), A/B 비교로 효과 확인
- [x] **③ 1인칭 편지 모드 smoke test** — 기본 6케이스 3인칭 가드레일 통과, 1인칭 3케이스 출력·위기차단 확인
- [x] **회복 추이(recovery_trend) 반영** — ⑤ 미션 난이도 추이 보정(`회복 중`↑/`주의 필요`↓), ③ 메시지 톤 캘리브레이션(위기 선체크 이후), `validate_recovery_trend.py` 5종 통과+LLM 톤 비교, 하위호환

### 막힌 것 / 고민
- Gemini 무료 티어 일일 한도(20회) — 키 교체로 해결
- 분당 5회 한도 → 딜레이 4초 부족, 15초로 조정
- eval이 운영 corpus.json을 아직 안 봄 → 정환주님 선행 필요 (corpus 작업 후 품질 확인 불가)

### 다음에 해야 할 일

**기능 (내 작업) — 우선순위 순**
- [x] **L1/L2/L3 차등 응답 라우팅 + 복지자원 안내** — 완료(→ Day 6)
- [ ] **질병 triage RAG 신규 구축** — ✅범위 확정("기본 처치+내원 유도", 상세 진단·처방 ❌) → 진행 가능. `vet_protocol` 코퍼스 + `triage.py` RAG 배선(현재 미사용) + 프롬프트 + 검증 스크립트 (大)
- [ ] **미션 RAG 코퍼스 보강** — `corpus.json` `mission` 5건·단일 토픽뿐 → 확장 + `validate_mission_rag.py` (中)
- [ ] **3단계 기념일 케어 알림** — 코드·테스트(`anniversary.py`)는 됐고 **백엔드 스케줄러 연동만 남음** (모세종)
- [ ] **유저 편지→AI 답장 (신규, 설계만 합의)** — 회복 절대점수 게이트(1인칭 AND 조건) + 편지 위기 선체크(L1 융합). 미정 4개 먼저 결정: THRESHOLD·stability 정의·데이터 최소기준·두 번째 축

**팀 협의 — 합의 완료 (06-08)**
- [x] recovery_trend 경로 통합 → **A안 확정.** 김윤한이 backend `message.py`를 `ai/llm.generate_message()` 호출로 수정 (ai/llm 준비 끝 → 연동 후 동작 확인만)
- [x] 미션 recovery_trend 연결 → 김윤한이 backend `mission.py`에 trend 계산 추가
- [x] triage 범위 → CLAUDE.md 기준대로 확정("기본 처치+내원 유도"). ETHICS 문서 한 줄 추가는 PM

---

## 2026-06-04 (Day 4)

### 한 일
- [x] **⑤ 미션 추천** — 감정 점수 → 난이도 규칙 + LLM 개인화 + 규칙 폴백, 테스트 10종(115종 통과)
- [x] **⑦ LLM 분류 레이어(L1)** — `classify_with_llm()` + `assess_crisis()` 보수적 융합(`max(L0, L1)`), 미탐 0 보장, 테스트 10종(125종 통과)
- [x] `validate_crisis_llm.py` — 실제 Gemini 골든셋 수동 검증 도구

### 결정 사항
- L1 활성화는 팀 결정 — 백엔드가 `assess_crisis(note, generate=provider.generate)` 로 호출 시 켜짐

### 막힌 것 / 고민
- Gemini 무료 티어 레이트리밋 → 폴백으로 흡수(서비스 무영향), 시연 시 호출 텀 필요
- 미션 출력 스키마 → 모세종님이 `MissionItem` 추가로 해결(PR #54)

### 추가 개선 작업
- [x] **⑤ 미션 규칙 풀 확장** — 난이도별 5→10개(총 30개), 반복 추천 감소
- [x] **⑦ 위기 감지 골든셋 20→30개 확장** — 완곡 표현·함정 케이스 추가, 신호 사전 보강(깨지않았으면·끝났으면좋겠·방법도다정해 등), L0 미탐 0·과탐 0, 159종 통과
- [x] **③ 추모 메시지 품질 검증** — `validate_memorial.py` 신규. 실제 Gemini 6종(강아지·고양이·토끼·위기 차단) 전부 가드레일 통과 확인

---

## 2026-06-03 (Day 3)

### 한 일 (무엇이 바뀌었나)
- [x] **`provider.py` 신규** — LLM 호출 추상화. OpenAI 호환 방식으로 Gemini 연결
  (`generate(prompt, *, max_tokens, temperature, json_mode)`), 재시도·타임아웃·예외 graceful
- [x] **`config.py` 신규** — `.env` 기반 설정(provider/base_url/model/key/타임아웃/재시도/토큰/온도)
- [x] **thinking 제어 추가** — `gemini-2.5-flash`는 thinking 기본 ON이라 답변이 잘림 →
  `reasoning_effort="none"`(config `LLM_REASONING_EFFORT`)으로 꺼서 해결
- [x] **`tests/test_provider.py` 신규** — 응답·오버라이드·json_mode·thinking·재시도·키누락 10종
- [x] **`memorial`이 실제 동작 확인** — `generate=provider.generate` 주입 →
  실제 Gemini로 봄이·초코 위로 메시지 생성, 위기 입력은 1393 우선, 부활 출력 차단까지 라이브 검증
- [x] **백엔드 API 스키마 합의·반영** — 리뷰 피드백 반영해 입출력 키를 백엔드 DB 기준으로 정렬
  - `years`(숫자) → **`period`**(문자열), `score` → **`emotion_score`**, 출력 `hotline` → **`crisis_message`**
  - 출력이 백엔드 `MessageResponse`(content·tone·source·risk_level·crisis_message)와 일치 → 모세종님이 stub을 `generate_message` 호출로 교체만 하면 연동 완료
- [x] 전체 테스트 84종 통과 / ruff·black 통과

### 결정 사항
- 엔진: **Gemini API**(OpenAI 호환 엔드포인트, `gemini-2.5-flash`). 로컬 EXAONE은 폴백
- `.env`에 Gemini 설정 채움 — **키는 로컬만**(`.gitignore`로 커밋 제외)
- ③ 입출력 키 = 백엔드 DB 스키마 기준(`pet.period`·`emotion_score`·`crisis_message`) — 백엔드팀 합의

### 막힌 것 / 고민
- Gemini 정식 확정은 골든셋(1인칭 회피·위기 미탐 0) 통과 확인 후 — 1인칭은 3샘플 통과, ⑦ LLM 분류 레이어에서 정식 검증 예정
- 톤 키(warm·calm·hopeful)가 정환주님 TTS 톤과 동일 → ③↔④ 값 합의 필요

### 다음 할 일
- [ ] ⑤ 미션 추천(`mission.py`) 또는 백엔드 ③ API 연동 협의

---

## 2026-06-02 (Day 2)

**컨디션:** AI 파트 본격 착수 🟨

### 오늘의 목표
- 위기 감정 감지(⑦) 규칙 레이어 고도화 + 골든 테스트셋 구축
- 추모 메시지(③) 프롬프트 초안 작성 + 생성 로직 흐름 잡기

### 한 일
- [x] **⑦ 위기 감지 규칙 고도화** — `safety.py` 규칙 레이어(L0)
  - 표현 대상(subject: self/pet/other) 구분으로 오탐 방지 ("봄이가 죽었어요"=정상 L0)
  - 본인 욕구 어법("~고 싶다") 신호 사전 + 맥락 점수(시제·부정·강도) 보정
  - 골든 테스트셋으로 미탐 0 회귀 검증, `CRISIS_HOTLINE`(1393) 상수화
- [x] **③ 추모 메시지 프롬프트 초안** — `prompts/memorial.py`
  - 시스템 프롬프트에 윤리 경계(반려동물 1인칭·부활 금지) 명시
  - 톤 3종(warm·calm·hopeful, TTS 톤 매핑 대비), 추억 키워드 자동 삽입
  - `build_user_prompt` / `build_messages` (system+user)
- [x] **③ 추모 메시지 생성 로직** — `memorial.py` `generate_message()`
  - 흐름: 위기 선체크 → 프롬프트 조립 → LLM 호출(주입) → 후처리 가드
  - 위기 신호(L2↑) 시 메시지 대신 1393 안내 우선
  - 부활/1인칭 출력 차단·재생성(`GuardrailViolation`)
- [x] **테스트** — `tests/test_memorial.py` 10종(프롬프트 조립·흐름·안전·가드), 전체 74종 통과

### 결정 사항
- `provider.generate`를 **의존성 주입**으로 비워둠 → 엔진 확정 전 가짜 모델로 흐름·가드 검증, 확정 후 그대로 꽂기
- 프롬프트는 코드와 분리해 `prompts/`에서 버전 관리 (팀 규칙)

### 막힌 것 / 고민
- **provider.py 미구현** — 진짜 LLM 호출은 선행결정 A(엔진/모델)·B(통합 방식)가 정해져야 가능. **정환주님과 협의 + GPU 서버(RTX 5060) 셋업** 필요
- ③ 메시지 "품질"(실제 위로 톤·1인칭 안 나오는지)은 모델 연결 후에야 검증 가능

### 내일 할 일
- [ ] PERSO API 문서 확인 (0-6) — 입/출력 형식·인증·할당량 파악
- [ ] 정환주님과 로컬 LLM 엔진/모델(결정 A)·통합 방식(결정 B) 협의
- [ ] 위 작업 `bansoram` → `dev` PR
