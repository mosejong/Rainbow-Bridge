## 2026-06-02 (Day 1)
### 오늘의 목표
- FastAPI 기본 코드 작성 및 MongoDB 연동
- 타임라인 API, LLM 사용량 API 완성

### 한 일
- [x] FastAPI 기본 코드 작성
- [x] MongoDB 연동 완료
- [x] ngrok 백그라운드 실행
- [x] uvicorn 백그라운드 실행 (~/Rainbow-Bridge/backend)
- [x] GitHub 팀 레포 클론 및 backend/kyh 브랜치 연동
- [x] PR 생성 완료
- [x] GET /api/v1/timeline/{pet_id} — 추모 타임라인 조회 API 완성
- [x] GET /api/v1/admin/usage — LLM 사용량 집계 API 완성
- [x] /docs 에서 두 API 정상 동작 확인
- [x] git pull origin dev 로 팀 전체 코드 동기화

### 막힌 것 / 고민
- uvicorn 실행 경로 문제 (~/Rainbow-Bridge vs ~/Rainbow-Bridge/backend) → 올바른 경로로 재시작해서 해결
- VSCode Claude가 메인컴에 파일 만들어서 서브컴에 없던 문제 → git pull로 해결
- sudo 비밀번호 3회 오류 → pgrep으로 우회해서 해결

### 내일 할 일
- [ ] 서비스 시나리오 방향 팀 확정 (목요일 6/5 데드라인)
- [ ] 카카오맵 API 키 발급
- [ ] GET /api/v1/hospitals — 동물병원 검색 API
- [ ] POST /api/v1/health — 증상·투약·검진 기록 저장 API
- [ ] MongoDB health_records 컬렉션 설계
- [ ] risk_level 스키마 수정 완료 후 타임라인 API 연동 확인

## 2026-06-03 (Day 2)
### 한 일
- [x] PR #34 반영 — PII 필드 제거, nohup.out gitignore, router.py 되돌리기
- [x] llm_log.py 스키마 재수정 (kind·latency_ms·ok 추가)
- [x] dev 브랜치 병합 + 충돌 해결
- [x] assess_crisis() 창구 함수 추가 + emotion 서비스 연동
- [x] GET /api/v1/hospitals 카카오맵 API 연동 완료 (테스트 확인)
- [x] 하드코딩 sys.path 제거 → app.core.ai_path 모듈로 교체

## 2026-06-04 (Day 3)
### 한 일
- [x] Docker 권한 문제 해결 (usermod -aG docker kyh)
- [x] Docker Compose 교체 완료 (MongoDB 볼륨 마운트 포함)
- [x] dev 브랜치 풀 + 충돌 해결 (hospitals.py, router.py)
- [x] L1 위기감지 generate 연동 (emotion.py, message.py)
- [x] Gemini API 키 .env 추가 (LLM_API_KEY·LLM_PROVIDER·LLM_BASE_URL·LLM_MODEL)
- [x] black 포맷 적용
- [x] Dockerfile 추가 (백엔드 컨테이너화 준비)
- [x] HDD 500GB 영구 마운트 (/mnt/hdd, 436GB 여유)
### 테스트 결과

## 2026-06-05 (Day 5)
### 한 일
- [x] media upload 검증 수정 (인증 복구, gif 제거, 에러메시지 통일) PR 제출
- [x] docker-compose.yml 백엔드 서비스 추가 + MongoDB 통합 실행
- [x] DuckDNS 도메인 설정 (rainbow-bridge.duckdns.org)
- [x] Let's Encrypt HTTPS 인증서 발급 + nginx 설정
- [x] MONGO_URI 컨테이너 이름으로 수정 (localhost → rainbow_mongo)
- [x] HTTPS API 정상 동작 확인 (hospitals API 97개 응답 확인)
- [x] `message.py` 컨플릭트 해결 (PR #111)
- [x] 미디어 파일 다운로드 API 추가 (`GET /api/v1/media/{asset_id}/download?type=video|voiced|dubbed`) (PR #111)
### 막힌 것 / 고민
- MongoDB localhost:27017 연결 오류 → MONGO_URI=mongodb://rainbow_mongo:27017 로 수정해서 해결
- .env gitignore라 직접 수정 필요했음
- Vet 모델 없음 → 팀장님 확인 중
### 다음 할 일
- [ ] 수의사 회원가입·로그인 API (Vet 모델 확인 후)
- [ ] PR #111 머지 후 NCP 배포

## 2026-06-08 (Day 8)

### 한 일

* git pull origin dev — PR #123(시장데이터), #124(멀티모달 윤리), #125(기능정의서+스크럼), #126(정환주), #127(소람님 L1 라우팅) 반영
* 기능정의서(FEATURE_SPEC.md) 확인 — 백엔드 파트 ①②③⑤⑥⑦ + 수의사 API 전부 완료 확인
* 스크럼 참석 — 강사님 피드백 수렴 (서비스 방향 재정의, TTS 감정 튜닝, 네이티브 앱 전환)
* PR #127(반소람) 머지 후 백엔드 L1 응답 스키마 연동 작업
  * schemas/message.py — MessageResponse에 L1 필드 추가 (support_message, welfare_resources: list[dict])
  * services/message.py — CrisisAction.BLOCK 기준으로 분기 수정, L1 응답 필드 세팅 추가
  * black 포매터 적용 (message.py, media.py)
* PR #128 생성 → CI 통과 → 머지 → NCP 배포 완료

### 막힌 것 / 고민

* CI black 오류 (media.py) → black 적용 후 재푸시로 해결
* services/message.py 들여쓰기 맞추는 데 시간 소요

### 다음 할 일

* 팀 서비스 방향 확정 후 추가 백엔드 작업 대기
* 소람님 welfare_resources featured/url 키 검증 — MVP 이후 필요 시 적용

## 2026-06-09 (Day 9)
### 한 일
- [x] TTS 서비스 Qwen3 GPU 서버 HTTP 연동 (B안, PR #155)
  - services/tts.py — Google TTS 단일 호출 → Qwen3 GPU 서버(B안) + Google TTS 폴백 구조로 교체
  - TTS_SERVER_URL 환경변수로 GPU 서버 연동
  - tone("warm"/"hopeful") → "girl", tone("calm") → "boy" 매핑 추가
  - black 포매터 통과
- [x] 영상 재생횟수 play_count + 로그인 접속 로그 추가 (강사님 피드백 반영)
  - endpoints/media.py — POST /{asset_id}/play 엔드포인트 추가
  - services/media.py — increment_play_count() 함수 추가
  - services/auth.py — 로그인 시 access_logs 컬렉션에 접속 기록 저장
- [x] build_report에 play_count·session_count 집계 추가
  - ai/evaluation/report.py — play_count, session_count 인자 추가
  - services/report.py — media_assets, access_logs DB 조회 후 집계 연동
  - schemas/report.py — play_count, session_count 필드 추가
- [x] NCP .env에 TTS_SERVER_URL 추가 후 재배포 → TTS 왕복 테스트 완료
### 막힌 것 / 고민
- guardian_nickname 전달 시 memorial.py 미지원으로 TypeError 발생 가능 → 이번 PR에서 제거, 소람님 memorial.py 수정 후 재연동 예정
- TTS 터널 URL ngrok 무료 1개 제한으로 LivePortrait와 충돌 → 경로 공유 프록시(B안)로 해결
### 다음 할 일
- [ ] 민수님 ngrok 터널 URL 받으면 NCP .env에 LIVEPORTRAIT_REMOTE_URL 추가 후 재배포
- [ ] 소람님 memorial.py guardian_nickname 추가 후 재연동
