# ===== 레인보우 브릿지 백엔드 의존성 =====
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
motor==3.6.0            # MongoDB 비동기 드라이버
python-dotenv==1.0.1
httpx==0.28.1           # 외부 API(PERSO/AI 서버) 호출
python-multipart==0.0.20  # 파일 업로드(UploadFile) 지원
openai==1.82.0            # Gemini OpenAI 호환 엔드포인트 호출
sqlalchemy==2.0.36        # RDB ORM
aiosqlite==0.20.0         # SQLite 비동기 드라이버 (로컬 개발)
asyncpg==0.30.0           # PostgreSQL 비동기 드라이버 (운영)
passlib[bcrypt]==1.7.4    # 비밀번호 해싱
python-jose[cryptography]==3.3.0  # JWT 토큰
google-cloud-texttospeech==2.36.0  # ④ TTS 음성 합성 (ai/tts 연동)

# ----- 개발/테스트 -----
pytest==8.3.4
pytest-asyncio==0.25.0
black==24.10.0
ruff==0.8.4
email-validator==2.2.0
gtts>=2.5
bcrypt==4.0.1
redis[asyncio]==5.2.1      # 감정 체크인 최근 기록 캐시
chromadb>=0.5
opencv-python-headless>=4.9  # 사진 적합도 채점 (선명도·밝기) — headless: GUI 불필요
mediapipe>=0.10              # 사진 얼굴 감지 보너스 채점 — 미설치 시 보너스 0으로 graceful 처리